{
  "name": "npdc-indicator",
  "version": "1.1.0",
  "description": "https://data.npolar.no/indicator",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "npolar",
  "repository": {
    "type": "git",
    "url": "git://github.com/npolar/npdc-indicator.git"
  },
  "license": "MIT",
  "dependencies": {
    "npdc-common": "npolar/npdc-common#v4.8.0"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.3.13",
    "gulp": "^3.9.0",
    "should": "^11",
    "npdc-gulp": "npolar/npdc-gulp",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "browserify-ngannotate": "^2.0.0",
    "partialify": "^3.1.3"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "es2015"
          ]
        }
      ],
      "partialify",
      "browserify-ngannotate"
    ]
  }
}
